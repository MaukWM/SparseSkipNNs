from torch.utils.data import Dataset, DataLoader, random_split

# TODO: Each different dataset has a different class, so that we can generalize the plot_model_distribution etc.
class CIFAR10(Dataset):

    pass